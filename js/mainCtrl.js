/*global window */
(function (window, angular) {
    'use strict';

    angular
        .module(window.appName)
        .controller('mainCtrl', ['$scope', 
            function ($scope) {
               
            }]);
}(window, window.angular));